# Database Configuration (matches oms-backend default local DB)
DB_HOST=localhost
DB_PORT=5432
DB_USER=oms_user
DB_PASSWORD=oms_password
DB_NAME=oms_database

# AI Model Configuration
# You must provide an OpenAI API key or use a compatible provider.
OPENAI_API_KEY=OPENAI_API_KEY